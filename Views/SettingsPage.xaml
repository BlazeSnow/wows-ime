<?xml version="1.0" encoding="UTF-8" ?>
<Page
    x:Class="wows_ime.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:toolkit="using:CommunityToolkit.WinUI.Controls"
    mc:Ignorable="d">

    <Page.Resources>
        <ContentDialog
            x:Name="ErrorDialog"
            CloseButtonText="OK" />
    </Page.Resources>

    <Page.Content>
        <ScrollViewer
            VerticalScrollMode="Enabled"
            VerticalScrollBarVisibility="Auto"
            HorizontalScrollMode="Disabled"
            HorizontalScrollBarVisibility="Disabled">
        <Grid Padding="20" RowSpacing="8" Background="Transparent">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <toolkit:SettingsCard
                Grid.Row="0"
                x:Uid="ProjectWebsiteCard">
                <toolkit:SettingsCard.HeaderIcon>
                    <FontIcon Glyph="&#xE774;" />
                </toolkit:SettingsCard.HeaderIcon>
                <toolkit:SettingsCard.Header>
                    <TextBlock x:Uid="ProjectWebsiteCardHeader" />
                </toolkit:SettingsCard.Header>
                <Button x:Uid="OpenProjectWebsiteButton" Click="OpenProjectWebsiteButton_Click" />
            </toolkit:SettingsCard>

            <toolkit:SettingsCard
                Grid.Row="1"
                x:Uid="ProjectRepositoryCard">
                <toolkit:SettingsCard.HeaderIcon>
                    <FontIcon Glyph="&#xE943;" />
                </toolkit:SettingsCard.HeaderIcon>
                <toolkit:SettingsCard.Header>
                    <TextBlock x:Uid="ProjectRepositoryCardHeader" />
                </toolkit:SettingsCard.Header>
                <Button x:Uid="OpenProjectRepositoryButton" Click="OpenProjectRepositoryButton_Click" />
            </toolkit:SettingsCard>

            <toolkit:SettingsCard Grid.Row="2">
                <toolkit:SettingsCard.HeaderIcon>
                    <FontIcon Glyph="&#xE946;" />
                </toolkit:SettingsCard.HeaderIcon>
                <toolkit:SettingsCard.Header>
                    <TextBlock x:Uid="AppVersionCardHeader" />
                </toolkit:SettingsCard.Header>
                <TextBlock Text="{x:Bind AppVersionText}" />
            </toolkit:SettingsCard>
        </Grid>
        </ScrollViewer>
    </Page.Content>
</Page>
